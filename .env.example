# Salin file ini menjadi `.env` (di folder yang sama dengan file ini),
# lalu isi 3 nilai dari https://dev.mendeley.com/ (My Apps).
# JANGAN pernah commit `.env` (sudah di-.gitignore).
MENDELEY_CLIENT_ID=
MENDELEY_CLIENT_SECRET=
MENDELEY_REDIRECT_URI=http://localhost:5000/oauth/callback
